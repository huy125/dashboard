import Navbar from "@/app/components/Header/Navbar";

const Header: React.FC = () => {
    return (
        <header>
            <div>
                <Navbar/>
            </div>
        </header>
    );
};

export default Header;
