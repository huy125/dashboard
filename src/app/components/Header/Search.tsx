const Search: React.FC = () => {
    return (
        <div>
            <input type='text' placeholder='Search...'/>
            <button>Search</button>
        </div>
    );
};

export default Search;
